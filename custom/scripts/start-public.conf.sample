# start-public.sh 設定檔樣本（把這檔案複製到以下任一位置即可被自動載入）
# 優先順序：
# 1) 環境變數 START_PUBLIC_CONFIG 指向的檔案
# 2) 專案根目錄：.start-public.conf
# 3) 此目錄：custom/scripts/start-public.conf
# 4) 使用者設定：$HOME/.config/ytmd-start-public.conf

# --- 前端/後端連線與埠 ---
FRONTEND_PORT=5173
WEB_SERVER_PORT=8080
UPSTREAM_HOST=localhost

# --- 公開隧道選擇 ---
# 預設使用 ngrok；Cloudflare 僅在提供 Named Tunnel 設定時啟用。
ENABLE_NGROK=1
ENABLE_CLOUDFLARE=0

# ngrok 設定
NGROK_BIN=ngrok
NGROK_REGION=jp
NGROK_ALWAYS_NEW=0

# Cloudflare 命名隧道（選用）
ENABLE_NAMED_TUNNEL=0
CLOUDFLARED_BIN=cloudflared
# 若要啟用，提供以下其中之一：
# CF_TUNNEL_TOKEN=xxxxx
# CF_TUNNEL_NAME=song-server
# 若未提供 hostname，可透過 config 解析 ingress；否則請顯式提供：
# CF_TUNNEL_CONFIG=$HOME/.cloudflared/config.yml
# CF_HOSTNAME_FRONTEND=vite.bc-verse.com
# CF_HOSTNAME_BACKEND=api.bc-verse.com

# --- 前端啟動模式與監控 ---
FRONTEND_MODE=auto   # auto|vite|static
FRONTEND_LOG=/tmp/ytreq-frontend.log
ENABLE_FRONTEND_SUPERVISOR=1

# --- 後端 ---
FORCE_BACKEND_RESTART=0

# --- QR 選項 ---
QR_PREFER_LAN=0        # 1: QR 使用區域網 IP
AUTO_INSTALL_QRCODE=1  # 自動安裝 qrcode[pil]

# --- Electron ---
ELECTRON_DEV=1

# --- 其他 ---
# PUBLIC_LINKS_PATH 可自訂 public_links.json 寫入路徑
